######## Window rules ########
windowrule = no_blur on, match:title .*
windowrule = opacity 0.95 override, match:title .*  # Applies transparency to EVERY WINDOW

# windowrule = float, match:class ^(blueberry.py)$
# windowrule = float, match:class ^(steam)$
# windowrule = float, match:class ^(guifetch)$  # FlafyDev/guifetch

# Warp
windowrule = tile on, match:class dev.warp.Warp

# Picture-in-Picture
windowrule = float on, match:title ^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$

# Center common dialogs
windowrule = center on, match:title ^(Open File)(.*)$
windowrule = center on, match:title ^(Select a File)(.*)$
windowrule = center on, match:title ^(Choose wallpaper)(.*)$
windowrule = center on, match:title ^(Open Folder)(.*)$
windowrule = center on, match:title ^(Save As)(.*)$
windowrule = center on, match:title ^(Library)(.*)$
windowrule = center on, match:title ^(File Upload)(.*)$

# windowrule = no_initial_focus, match:title .*

# Dialogs (floating)
windowrule = float on, match:title ^(Open File)(.*)$
windowrule = float on, match:title ^(Select a File)(.*)$
windowrule = float on, match:title ^(Choose wallpaper)(.*)$
windowrule = float on, match:title ^(Open Folder)(.*)$
windowrule = float on, match:title ^(Save As)(.*)$
windowrule = float on, match:title ^(Library)(.*)$
windowrule = float on, match:title ^(File Upload)(.*)$

# Tearing
windowrule = immediate on, match:title .*\.exe
windowrule = immediate on, match:class steam_app

# No shadow for tiled windows
windowrule = no_shadow on, match:float 0


######## Layer rules ########
layerrule = xray 1, match:namespace .*
# layerrule = no_anim, match:namespace .*   # Example: disable all layer animations

layerrule = no_anim on, match:namespace walker
layerrule = no_anim on, match:namespace selection
layerrule = no_anim on, match:namespace overview
layerrule = no_anim on, match:namespace anyrun
layerrule = no_anim on, match:namespace indicator.*
layerrule = no_anim on, match:namespace osk
layerrule = no_anim on, match:namespace hyprpicker

layerrule = blur on, match:namespace shell:.*
layerrule = ignore_alpha 0.6, match:namespace shell:.*

layerrule = no_anim on, match:namespace noanim
layerrule = blur on, match:namespace gtk-layer-shell
layerrule = ignore_alpha 0, match:namespace gtk-layer-shell

layerrule = blur on, match:namespace launcher
layerrule = ignore_alpha 0.5, match:namespace launcher

layerrule = blur on, match:namespace notifications
layerrule = ignore_alpha 0.69, match:namespace notifications

# ags / bars / docks etc.
layerrule = animation slide top, match:namespace sideleft.*
layerrule = animation slide top, match:namespace sideright.*
layerrule = blur on, match:namespace session

layerrule = blur on, match:namespace bar
layerrule = ignore_alpha 0.6, match:namespace bar

layerrule = blur on, match:namespace corner.*
layerrule = ignore_alpha 0.6, match:namespace corner.*

layerrule = blur on, match:namespace dock
layerrule = ignore_alpha 0.6, match:namespace dock

layerrule = blur on, match:namespace indicator.*
layerrule = ignore_alpha 0.6, match:namespace indicator.*

layerrule = blur on, match:namespace overview
layerrule = ignore_alpha 0.6, match:namespace overview

layerrule = blur on, match:namespace cheatsheet
layerrule = ignore_alpha 0.6, match:namespace cheatsheet

layerrule = blur on, match:namespace sideright
layerrule = ignore_alpha 0.6, match:namespace sideright

layerrule = blur on, match:namespace sideleft
layerrule = ignore_alpha 0.6, match:namespace sideleft

layerrule = blur on, match:namespace indicator*
layerrule = ignore_alpha 0.6, match:namespace indicator*

layerrule = blur on, match:namespace osk
layerrule = ignore_alpha 0.6, match:namespace osk


######### USER rules ########
# Spotify
# windowrule = no_initial_focus, match:class Spotify
# windowrule = workspace 10, match:class Spotify

# Packet Tracer
windowrule = no_initial_focus on, match:class PacketTracer

# DBeaver
windowrule = no_initial_focus on, match:class DBeaver

# Beat Saber Oneclick helper
windowrule = float size 180 100, match:class bs-oneclick.py

# FreeCAD
windowrule = no_initial_focus on suppress_event on activatefocus on, match:class FreeCAD

# Android Studio
windowrule = no_initial_focus on, match:class jetbrains-studio

# KDE Connect
windowrule = no_initial_focus on, match:class org.kde.kdeconnect.daemon
windowrule = workspace 11, match:class org.kde.kdeconnect.daemon

# KiCad
windowrule = workspace 6, match:title .*— KiCad.*
windowrule = workspace 7, match:title .*Schematic Editor$
windowrule = workspace 8, match:title .*PCB Editor$
windowrule = float on workspace 8, match:class KiCad match:title 3D Viewer$
# windowrule = size 10% 100%, match:title kicad\-wakatime.*
windowrule = size 1280 1000, match:class kicad  # title:Footprint Chooser.*

# STM32CubeIDE
windowrule = stay_focused on, match:class org-eclipse-core-internal-jobs-Worker

# GIMP
windowrule = opacity 1 suppress_event activatefocus no_initial_focus, match:class gimp

